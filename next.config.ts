import type { NextConfig } from 'next'

/**
 * Next.js 16 配置入口（最小占位）。
 *
 * 当前只承载默认配置；后续如需加 middleware / image domains / 公开环境变量，
 * 在这里扩展并同步更新 docs。改动后必过 `pnpm build` + `pnpm test`。
 */
const nextConfig: NextConfig = {
  /* config options here */
}

export default nextConfig
