# 证据清单

按 P0 构建方案 §15 要求，严格区分以下证据等级。任何模拟结果、代码推导图片或固定样例都不得描述为真实运行成功。

## 证据等级定义

| 标签 | 含义 | 可信度 |
|---|---|---|
| [已实现] | 代码已写，typecheck/build 通过 | 代码存在 |
| [单元测试] | node --test 覆盖，pass | 逻辑正确 |
| [本地构建] | pnpm build 生产构建通过 | 可编译 |
| [本地浏览器] | pnpm dev 手动验证 | UI 可用 |
| [固定回放] | tests/fixtures 脱敏样例 | 非真实模型 |
| [真实模型调用] | 用真实 API Key 调 Qwen | ❌ 未做 |
| [线上部署] | 生产环境运行 | ❌ 未做 |
| [真实用户验证] | 老年用户可用性测试 | ❌ 未做 |

## 按 P0 路径的证据

### 路径 1：低风险（微信通话声音恢复）

| 环节 | 证据等级 | 说明 |
|---|---|---|
| 关键词分类「微信没声音」→ low | [单元测试] | classify-risk.test.ts 16 个验收 case |
| 路由到 /confirm | [单元测试] | user-routing.test.ts |
| 白名单教程匹配 | [单元测试] | tutorial.test.ts |
| 一步指导页面 | [本地构建] | tutorial 页面编译通过 |
| 语音播报 | [本地构建] | speak-button.tsx 编译通过（需浏览器验证 Web Speech） |

### 路径 2：中风险（电商退款）

| 环节 | 证据等级 | 说明 |
|---|---|---|
| 关键词分类 → medium | [单元测试] | classify-risk.test |
| 决策链 guide 分支 | [单元测试] | decide-next.test mock Vision |
| 截图观察 | [固定回放] | vision-fixtures.ts scenario=medium-risk-refund |
| 真实截图识别 | ❌ 未做 | 需真实 Qwen API Key |

### 路径 3：高风险（转账/验证码/屏幕共享）

| 环节 | 证据等级 | 说明 |
|---|---|---|
| 关键词分类 → critical | [单元测试] | classify-risk.test |
| 路由到 /risk-alert | [单元测试] | user-routing.test |
| 决策链 stop 分支 + 求助卡 | [单元测试] | decide-next.test |
| 求助卡脱敏序列化 | [单元测试] | card-serialization.test |
| 风险停止页面 | [本地构建] | risk-alert 页面编译通过 |
| 系统分享/复制 | [本地构建] | navigator.share 逻辑已写（需浏览器验证） |

## 决策链安全不变量证据

| 不变量 | 测试 | 文件 |
|---|---|---|
| 高风险绝不进 guide | decide-next.test「critical → stop」 | application/decide-next.test.ts |
| AI 不能降级风险 | risk-policy.test「rule high + vision low → high」 | contracts/risk-policy.test.ts |
| 视觉失败不 fail-open | decide-next.test「超时 → clarify」 | application/decide-next.test.ts |
| 高风险即使 vision 故障也 stop | decide-next.test「高风险优先于视觉故障」 | application/decide-next.test.ts |
| UNKNOWN 不显示按钮指导 | UNKNOWN 页面审查 | app/unknown/ |
| 求助卡不含教给出去话术 | handoff.test「安全约束」 | domain/handoff/handoff.test.ts |
| 审计日志不含原始文本 | decide-next.test「记录脱敏审计事件」 | application/decide-next.test.ts |
| 对比度符合 WCAG AA | contrast.test 设计令牌契约 | lib/a11y/contrast.test.ts |

## 已知限制（诚实声明）

1. **真实 Qwen 未调通**：adapter 已实现，但未用真实 Key 验证过端到端
2. **截图同意/涂抹 UI 未做**：方案 §7.1 的前端流程未实现，Route Handler 支持上传但无涂抹页
3. **完整状态机未做**：P0 用 tutorial 第一步替代，多步推进留待后续
4. **无 E2E 自动化**：方案 §12.2 的 Playwright E2E 未实现（门禁靠单测 + build）
5. **无线上部署**：仅本地验证
6. **无真实用户测试**：老年用户可用性未验证
