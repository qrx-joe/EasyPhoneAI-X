# tests/fixtures/ — 脱敏评测样例

固定的脱敏截图与策略评测样例。

## 职责（方案 7.3）

覆盖三条比赛路径和故障路径的固定样例，用于：
- 离线回归测试（无网络时的兜底验证）
- 状态机验证
- 没有网络时的演示兜底

## 证据口径（强制）

- 回放演示必须明确标注为**固定样例**，不得描述为实时模型结果。
- 真实 Qwen 调用、固定回放、代码推导图和设计稿分别保存证据标签。
- 任何模拟结果、固定样例都不得描述为真实运行成功（方案 15 节）。

P0 阶段的具体 fixtures 在第四阶段接入 Vision 后填充。
